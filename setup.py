from setuptools import setup

setup(
    name='dclnt',
    packages=['dclnt'],
    version="0.0.1",
    description="Natural language statistics for Python projects",
    install_requires=[
        'nltk',
    ],
    licence="Apache 2.0",

)
