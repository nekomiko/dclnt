from setuptools import setup

setup(
    name='nekolrep',
    packages=['nekolrep'],
    version="0.0.1",
    description="Natural language statistics for Python projects",
    install_requires=[
        'nltk',
        'GitPython',
    ],
    licence="Apache 2.0",

)
